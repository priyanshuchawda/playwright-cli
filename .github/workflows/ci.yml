# Automatic CI disabled — saves GitHub Actions minutes.
# Test on Fedora locally: npm ci && npm run test && scripts/setup-fedora.sh
name: CI

on:
  workflow_dispatch:

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "CI is disabled for this fork."
          echo "On Fedora: clone repo, run scripts/setup-fedora.sh"
          echo "See FEDORA.md"
